# Budget tracker

## Setup
1. Create venv.
2. Install requirements.txt.
3. Create .env file and insert enter hosts and secret key.
```
SECRET_KEY=""
HOSTS="127.0.0.1,localhost"
```

## To do
1. Figure out a way to store and update after change categories and subcategories dynamically within DOM for global page (settings.html) without reload - both update and delete modal windows